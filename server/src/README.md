# Usage
```bash
scraper users                   # list all users
scraper register <username>     # register a new user
scraper login <username>        # switches the current user

scraper reset                   # clears the database
```